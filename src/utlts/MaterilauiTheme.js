import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: "#312ECB",
    },
    secondary: {
      main: "#007AFF",
    },
    accent: {
      main: "#715DF1",
    },
    highlight: {
      main: "#ff8c42",
    },
    background: {
      default: "#DFDFDF",
    },
  },
});

export default theme;
